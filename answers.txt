What is the difference between a merge and a rebase?

Merge combines changes from two branches into a single commit while rebase creates a new set of commits to be applied on top of the target branch.

What is a remote repository?

Repositories that sit on a server and can be accessed by members of a team.
